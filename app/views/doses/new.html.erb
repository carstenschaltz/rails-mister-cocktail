<div class="container">
  <h1>New dose for <%= @cocktail.name %></h1>

  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.association :ingredient, input_html: { multiple: false } %>
    <%= f.input :description %>
    <%= f.submit %>
  <% end %>
</div>

